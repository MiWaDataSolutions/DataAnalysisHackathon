{
    "configurations": [
        {
            "name": "Python: Uvicorn",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "api.api:app",
                "--reload"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}",
                "ADK_LOG_LEVEL":"DEBUG"
            }
        }
    ]
}